name: Mobile CI

on:
  # Automatic mobile CI is paused while this fork focuses on the Rails app.
  workflow_dispatch:

concurrency:
  group: mobile-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
